# PRISM Environment Variables Template
# Copy to .env and fill in your values:  cp env_template.txt .env
# NEVER commit .env to version control!

# =============================================================================
# OpenAI (required)
# =============================================================================
# Get your key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-your-api-key-here

# Examples: gpt-4.1, gpt-4o, gpt-4o-mini (must match an OpenAI chat model id)
OPENAI_MODEL=gpt-4.1

# 0.0 = deterministic, 1.0 = creative
OPENAI_TEMPERATURE=0.0

OPENAI_MAX_TOKENS=1000

# =============================================================================
# Application
# =============================================================================
# development | staging | production
APP_ENV=development
DEBUG=true
LOG_LEVEL=INFO

# =============================================================================
# ML
# =============================================================================
ML_MODEL_VERSION=v1
ML_RANDOM_STATE=42

# =============================================================================
# LLM caching & reliability
# =============================================================================
LLM_CACHE_ENABLED=true
LLM_MAX_RETRIES=3
LLM_TIMEOUT=30

# =============================================================================
# Dashboard (Streamlit)
# =============================================================================
STREAMLIT_SERVER_PORT=8501
STREAMLIT_SERVER_ADDRESS=localhost

# =============================================================================
# Jira Cloud — OAuth 2.0 (3LO)
# =============================================================================
# Create an OAuth 2.0 (3LO) app: https://developer.atlassian.com/console/myapps
# Add callback URL to match your app (e.g. http://localhost:8501/ or the exact
# Upload Data page URL from the browser). Scopes below are typical for read-only.
JIRA_OAUTH_CLIENT_ID=
JIRA_OAUTH_CLIENT_SECRET=
JIRA_OAUTH_REDIRECT_URI=http://localhost:8501/
JIRA_OAUTH_SCOPES=read:jira-work read:jira-user offline_access
JIRA_MAX_ISSUES_PER_PROJECT=2000
